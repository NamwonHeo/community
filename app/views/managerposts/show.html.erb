<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @managerpost.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @managerpost.content %>
</p>


<% if current_user.has_role? :admin %>
  <p>
    <strong>Published:</strong>
    <%= @managerpost.published %>
  </p>
  <%= link_to 'Edit', edit_managerpost_path(@managerpost) %> |
	<%= link_to 'Delete', @post, method: :delete, data: { confirm: '정말로 삭제하시겠습니까?' } %> |
<% end %>
  <%= link_to 'Back', managerposts_path %>
